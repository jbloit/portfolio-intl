import React from 'react'
import Layout from "../layouts/en"

const IndexPage = (props) => (
  <Layout location={props.location}>
  <div><p>Stuff I'm working on</p></div>
        
  </Layout>
)

export default IndexPage
